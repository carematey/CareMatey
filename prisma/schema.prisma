generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Account {
  id                String  @id @default(cuid())
  userId            String
  type              String
  provider          String
  providerAccountId String
  refresh_token     String?
  access_token      String?
  expires_at        Int?
  token_type        String?
  scope             String?
  id_token          String?
  session_state     String?
  user              User    @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([provider, providerAccountId])
}

model Session {
  id           String   @id @default(cuid())
  sessionToken String   @unique
  userId       String
  expires      DateTime
  user         User     @relation(fields: [userId], references: [id], onDelete: Cascade)
}

model User {
  id                 String               @id @default(cuid())
  name               String?
  email              String?              @unique
  emailVerified      DateTime?
  image              String?
  accounts           Account[]
  sessions           Session[]
  spaceAuthorization SpaceAuthorization[]
  card               Card[]
  space              Space[]
}

model VerificationToken {
  identifier String
  token      String   @unique
  expires    DateTime

  @@unique([identifier, token])
}

model Space {
  id                 Int                  @id @default(autoincrement())
  name               String?
  createdAt          DateTime             @default(now())
  updatedAt          DateTime?            @default(now())
  archivedAt         DateTime?
  deletedAt          DateTime?
  userId             String?
  spaceAuthorization SpaceAuthorization[]
  cards              Card[]
  user               User?                @relation(fields: [userId], references: [id])
  categories         Category[]
  categoryId         Int?
}

model Card {
  id         Int       @id @default(autoincrement())
  spaceId    Int
  title      String
  text       String
  tags       String[]
  ownerId    String
  creatorId  String
  createdAt  DateTime  @default(now())
  updatedAt  DateTime? @default(now())
  archivedAt DateTime?
  deletedAt  DateTime?
  userId     String?
  space      Space     @relation(fields: [spaceId], references: [id])
  user       User?     @relation(fields: [userId], references: [id])
  category   Category? @relation(fields: [categoryId], references: [id])
  categoryId Int?

  @@index([tags, creatorId])
}

model SpaceAuthorization {
  id               Int       @id @default(autoincrement())
  spaceId          Int
  spaceName        String?
  role             String    @default("READ")
  authorizedUserId String
  createdAt        DateTime  @default(now())
  updatedAt        DateTime? @default(now())
  deletedAt        DateTime?
  authorizedUser   User      @relation(fields: [authorizedUserId], references: [id])
  space            Space     @relation(fields: [spaceId], references: [id])
}

model Category {
  id                 Int     @id @unique @default(autoincrement())
  Name               String
  description        String?
  displayDescription Boolean @default(true)
  spaceId            Int
  Space              Space   @relation(fields: [spaceId], references: [id])
  cards              Card[]
}

//* To push db changes - https://www.prisma.io/docs/concepts/components/prisma-migrate/migrate-development-production
// comment out DB_URL in .env
// uncomment the Local Database section in .env
// Run the database in Docker (~docker-compose up)
// verify the database is valid with npm run dev and create some content.
// run npx prisma migrate dev
// if everything goes well, uncomment the correct DB_URL in .env
// run npx prisma migrate deploy
// if everything goes well do the same for the production database
//
// errors - 
// npx prisma migrate reset - will undo the committed change
// you can edit a migration file to handle errors
//
//* you must mark each errored migration as resolved or rolled-back - https://pris.ly/d/migrate-resolve
